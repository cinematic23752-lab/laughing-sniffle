import { Routes, Route } from 'react-router-dom';

// تعريف مكونات مؤقتة (Placeholder Components)
// في التطبيق الحقيقي، يجب أن تقوم باستيراد هذه المكونات من ملفاتها الخاصة (مثل ./pages/Home.tsx)
const Home = () => <h1>🏠 الصفحة الرئيسية</h1>;
const Dashboard = () => <h1>📊 لوحة التحكم</h1>;
const NotFound = () => <h1>404 - 😟 الصفحة غير موجودة</h1>;


function App() {
  return (
    <div className="App">
      {/* هنا يمكنك وضع أي مكونات تريدها أن تظهر في جميع صفحات التطبيق،
        مثل شريط التنقل (Navbar) أو التذييل (Footer).
      */}
      
      {/* مكون Routes يغلف جميع تعاريف المسارات. 
        يتم عرض مكون Route واحد فقط في كل مرة.
      */}
      <Routes>
        {/* المسار الأساسي (عند زيارة /) */}
        <Route path="/" element={<Home />} /> 
        
        {/* مسار لوحة التحكم (عند زيارة /dashboard) */}
        <Route path="/dashboard" element={<Dashboard />} />
        
        {/* مسار Catch-All: يُعرض لأي عنوان URL لا يتطابق مع المسارات المذكورة أعلاه */}
        <Route path="*" element={<NotFound />} />
      </Routes>
    </div>
  );
}

export default App;